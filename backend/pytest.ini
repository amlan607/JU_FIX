[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    unit: Unit tests for services and business rules.
    api: Endpoint level tests using the FastAPI TestClient.
